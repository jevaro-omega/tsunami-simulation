# tsunami-simulation
It is a prototype, so the settings in the simulation are “somewhat” limited. A recommendation for the parameters to set.  Intensity &lt; 1 Base speed &lt; 1.5 Damping = 0.9995 Topography = selectable Water = selectable Reset completely resets all values New topography = selectable
